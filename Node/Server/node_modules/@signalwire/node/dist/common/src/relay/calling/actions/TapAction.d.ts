import { BaseAction } from './BaseAction';
import { Tap } from '../components/Tap';
import { TapResult } from '../results/TapResult';
import { IRelayCallingTapDevice } from '../../../util/interfaces';
export declare class TapAction extends BaseAction {
    component: Tap;
    constructor(component: Tap);
    readonly result: TapResult;
    readonly sourceDevice: IRelayCallingTapDevice;
    stop(): Promise<import("../results").StopResult>;
}
