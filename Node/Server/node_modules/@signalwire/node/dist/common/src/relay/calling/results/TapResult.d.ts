import { BaseResult } from './BaseResult';
import { Tap } from '../components/Tap';
import { IRelayCallingTapTap, IRelayCallingTapDevice } from '../../../util/interfaces';
export declare class TapResult extends BaseResult {
    component: Tap;
    constructor(component: Tap);
    readonly tap: IRelayCallingTapTap;
    readonly sourceDevice: IRelayCallingTapDevice;
    readonly destinationDevice: IRelayCallingTapDevice;
}
